
// This file is no longer actively used as the navigation is simplified
// for the new single-page promotional website. The links are now managed
// directly in the header within `src/app/page.tsx`.

export function getNavLinks(pathname: string) {
    // Returns an empty array as there's no dynamic nav bar.
    return [];
}
