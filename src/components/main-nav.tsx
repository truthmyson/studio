
'use client';

// This component is no longer the primary navigation source.
// The new website has its navigation hardcoded in the header on the main page.
// Keeping this file in case a multi-page structure is needed again in the future.
export function MainNav({ className, ...props }: React.HTMLAttributes<HTMLElement>) {
    return null;
}
